# MavenAI

**Сборка с умом. Диагностика с душой.**

# MavenAI — умный ИИ-плагин для Apache Maven

**MavenAI** — это расширенный искусственный интеллект для разработчиков, который усиливает Apache Maven, добавляя:

- автоматическую диагностику ошибок,
- умный анализ зависимостей,
- оптимизацию сборки,
- сканирование pom.xml,
- режим собеседника,
- интеграцию с IDE.

---

## 🔧 Команды MavenAI

### `mvn ai:errdebug`

Анализирует логи сборки, устанавливает источник ошибки, предлагает исправления.

### `mvn ai:dependency-checker`

Определяет конфликтующие зависимости, указывает дубликаты, версии и предлагает замену.

### `mvn ai:smart-build`

Запускает сборку в «умном режиме». Искусственный интеллект:

- прогнозирует ошибки,
- оптимизирует порядок задач,
- предупреждает о потенциальных проблемах.

### `mvn ai:code-inspector`

Использует lightweight-анализатор для проверки исходного кода и pom.xml.

---

## 🧠 Архитектура MavenAI

MavenAI состоит из трёх основных модулей:

- **mavenai-core** — ядро, анализатор, парсер логов, система рекомендаций.
- **mavenai-plugin** — стандартный Maven-плагин, добавляющий команды ai:\*.
- **mavenai-agent** — легковесный локальный сервис, ускоряющий обработку.

---

## 📦 Сборка проекта

mvn clean install

---

## ⚖️ Лицензия

MIT License
